# HPA Manifests

This directory contains Kubernetes manifests or manifest placeholders related to Horizontal Pod Autoscaler configuration.

Typical resources may include:

- autoscaling/v2 HPA definitions
- target deployment references
- CPU and memory threshold configuration

These manifests demonstrate how metric-driven autoscaling is represented declaratively in Kubernetes.